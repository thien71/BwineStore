@model  WebsiteBanRuouVang.Models.Departments
@{
    ViewBag.Title = "Sửa danh mục";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
    var cookie = Context.Request.Cookies["auth"];
    ViewBag.Username = cookie.Value;
    ViewBag.Username = cookie.Value;
}

<div class="create-product">
    <div class="create-product-header">
        <h2>@ViewBag.Title</h2>
    </div>

    <div class="create-product-content">
        @using (Html.BeginForm("Edit", "Departments", FormMethod.Post))
        {
            <div class="create-product-content-top">
                <div class="create-dep-content-item">
                    @Html.LabelFor(model => model.Name, "Tên danh mục")
                    @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
                </div>
            </div>

            <div class="create-product-content-bottom edit-product-content-bottom">
                <button type="submit" class="btn btn-primary">Sửa</button>
                <button type="button" class="btn btn-primary"><a href="/departments/index">Huỷ</a></button>
            </div>
        }
    </div>
</div>